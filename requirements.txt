# Core agent stack
langgraph>=1.2,<2
langgraph-checkpoint-sqlite>=2.0
langchain-core>=1.0

# Model access
groq>=0.31

# Web
flask>=3.0
flask-cors>=4.0
python-dotenv>=1.0
gunicorn>=21.2          # WSGI server used by Azure App Service to run app:app

# Document loading (optional - the bot falls back to a built-in corpus)
pypdf>=5.0

# Optional: the semantic half of hybrid retrieval. Omit on constrained hosts;
# retrieval degrades to keyword-only by design.
# sentence-transformers>=3.0
